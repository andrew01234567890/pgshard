---
title: Implementation status
description: Evidence-based status of the Milestone 1 design and runtime.
---

# Implementation status

Milestone 1 is under active development. Design contracts describe the intended
end state; they are not claims that a feature is available. This page is updated
in the same pull request whenever implementation status changes.

The current Pod-lifecycle slice cryptographically authenticates cluster and
terminal receipts. Binding and status each have a final validator after their
mutator, and PostgreSQL startup fails before PGDATA access when binding-time
Node evidence is absent. The namespace handshake proves the selected admission
path handled a fresh challenge; it is not evidence that every API-server
selector cache converged simultaneously.

| Area | Current evidence | Status |
|---|---|---|
| Core key ranges and routing hash | Rust types, no-allocation hash, golden vectors, microbenchmark | Implemented in source |
| Control and change-stream contracts | Buf-linted alpha protobuf definitions | Implemented in source |
| Public-repository, CI and release policy | Parallel CI, binary-safe audited-range history privacy audit, source-only SemVer tooling | Implemented in source |
| Documentation site | Typed Docusaurus build and link validation | Implemented in source |
| Local container images | Digest-pinned Linux/amd64 Rust runtime, Go operator, and PostgreSQL 18 agent Docker-compatible image archives, non-root entrypoint smoke tests, no registry output | Implemented in source; test artifacts only |
| Go operator API and supporting resources | Defaulting/validation, generated CRD/RBAC/webhook, immutable content-addressed common and per-member PostgreSQL 18 role ConfigMaps, Services/workloads/HPA/PDB/NetworkPolicy, create-safe server-side-apply ownership and migration, authoritative workload observation, namespace-scoped leader election, uncached finalizer proofs, storage-policy snapshots, and exact-UID PVC retention or deletion. One-member asynchronous topology creates digest-pinned PostgreSQL 18 primaries with randomly named credential Secrets. The Secret UID and its detachment from cluster GC are checkpointed before it becomes every PVC create's durable owner. After the exact PVC UID is checkpointed, the live claim receives a controller protection finalizer and becomes ownerless while the Secret tombstone is anchored back to it; workloads are published only after this inversion. Before publication, a challenge receipt proves the Pod-binding namespace selector is active and namespace admission makes that opt-in immutable. Every managed PostgreSQL Pod is cluster-UID-bound; binding admission atomically pins the selected Node UID and boot ID, status admission protects its identity and finalizer, and the termination finalizer is released only for a durable terminal condition added to the exact authenticated kubelet update. A PodGC-authored phase does not satisfy the fence. Finalization verifies that receipt before pruning workload controllers, resolves and protects or abandons every possible PVC-create outcome while the exact credential fence remains, observes each credential fence absent, and then proves authoritative absence of every Pod mounting the checkpointed data PVC before releasing retained storage or completing deletion. Credential-only clients do not own PGDATA and do not block that barrier after PostgreSQL termination is proved. It retains or deletes only the resulting exact UID, honors explicit PVC deletion, and never creates replacement storage during finalization. Atomic cluster-and-shard-bound initialization uses targeted file and directory publication barriers and repeats the final-data and parent-directory barrier on the validated restart path. Kubernetes coverage includes admission, immutable namespace and status metadata rejection, apply/delete/recreate, live-PVC survival after tombstone deletion, late-create GC, authorized and denied NetworkPolicy traffic, destination-credential shard SQL, explicit-delete Retain finalization, late-Pod retention fencing, binding-identity and authenticated force-delete receipt recovery across a webhook outage, foreground-delete retention, and restart persistence | Partial; direct single-member primaries only |
| Rust agent and orchestrator foundations | Linux HTTP health/readiness/status/metrics with pending-network-error retry, quiet-interval streak reset, and a terminal 30-second rapid-failure bound, exact integer reporting, validated default and maximum lease TTLs, monotonic elapsed-time lease liveness with conservative dual-wall sampling, informational acquisition handles that require exact term/deadline/catalog/fence revalidation at dispatch, Unix timestamps retained only for external validation and reporting, and opt-in PostgreSQL 18 offline validation plus process-tree quarantine supervision with a dedicated Linux child subreaper; target-side epoch fencing and orchestrator persistence remain disabled | Implemented in source; deliberately not ready for control traffic |
| PostgreSQL lifecycle and HA | The opt-in local agent performs strict PostgreSQL 18 storage validation, control-file inspection, cross-namespace PGDATA locking, pidfd/process-group supervision, child-subreaper cleanup including escaped `setsid()` descendants, and fail-closed recovery-state handling. Unit, Linux production-binary, and real-container tests cover cancellation, same-PGDATA exclusion, unsafe paths and mounts, recovery rejection, lock evidence, descendant reaping, slot/WAL preservation, crash/restart, and clean shutdown. The operator separately bootstraps direct single-member PostgreSQL 18 primaries through cluster-and-shard-bound atomic staging initialization. PVC creation is fenced by the exact Secret UID; before workload publication the exact PVC becomes ownerless and controller-finalized, and the Secret becomes its dependent tombstone. Their Pod lifecycle pins the binding-time Node incarnation and requires an admission-attested kubelet terminal condition; it fails closed for PodGC, admission outage, missing/rebooted/replaced Nodes, but remains lifecycle evidence rather than physical node fencing and does not use the agent quarantine runtime yet. Physical replication, role-aware recovery, client activation, promotion, replicated operator-managed Pods, and a restart controller are absent | Partial; single-member lifecycle and local quarantine boundaries only |
| Pooling and SQL routing | A Linux runtime composing catalog supervision; bounded HTTP health/readiness/status and Prometheus publication; and a connection-, packet-, time-, and drain-bounded read-write PostgreSQL handshake listener that refuses GSS/SSL, closes cancellation silently, and returns `FATAL`/`57P03` to regular startup. Regular-file and timing bounds, pending-network-error retry, quiet-interval streak reset, a terminal 30-second rapid-failure ceiling, joint listener supervision that removes false-positive health after client-listener failure and preserves simultaneous failures, hard child-task shutdown, and process-level `SIGTERM` coverage are implemented; overall readiness deliberately remains false without a SQL data plane. Also implemented: bounded zero-copy PostgreSQL 18 frontend/backend framing, fixed SSL/GSS startup requests, caller-buffered regular startup packets, protocol-proof-bound cancellation request encoding, selected frontend bodies including statement/portal `Describe` and `Close`, exact borrowed backend `ParameterDescription`, UTF-8 `ParameterStatus`, opaque `BackendKeyData`, typed redacted authentication requests, encoder-proof-bound frontend SCRAM initial/follow-up responses under PostgreSQL 18's 1,024-byte bound, closed ordered SCRAM advertisements and phase-proof-bound continue/final encoders, minimal client-facing `ErrorResponse` encoding with required canonical fields, exact protocol-negotiation responses, a linear validator bound to the outbound startup parameters, exact PostgreSQL 18 protocol-specific cancellation-key lengths, empty completion validation, `ReadyForQuery` transaction status, allocation-free fixed startup-control encoders, caller-buffered variable startup-control encoders with all-or-nothing validation, byte/token/AST/stack-bounded permissive candidate parsing with a live PostgreSQL 18 positive/known-negative smoke corpus and bounded arbitrary-Unicode properties, a catalog-bound template for one deliberately narrow parameterized `SELECT` shape with property coverage across legal parameter numbers, operand orientation, parentheses, and rejected noncanonical placeholders, exact PostgreSQL parameter-type resolution gated by cluster/snapshot-bound all-active-shard proof of exact permanent non-inherited relation identity, type, collation, encoding, and schema epoch, live wrong-relation/view/unlogged/inheritance/partition/coercion, empty-path, and cached-plan re-analysis boundary tests, private fail-closed composition of the resolved proof with zero-copy Bind count/format/NULL/value validation and canonical hashing plus randomized properties for supported encodings, malformed widths, ranges, and selected Bind parameters, fallible validated-message iterators that surface private invariant failures, four parallel cargo-fuzz decoder targets with a committed valid corpus and a pgoutput configuration matrix, and standalone parser, wire-codec, and routing-hash microbenchmarks. Public routing remains sealed until an authoritative connection-owning physical-schema reader exists; no accepted PostgreSQL session, TLS/authentication policy or cryptographic exchange, optional diagnostic fields, complete backend phase/session state, socket-bound cancellation routing, query-cycle tracking, statement/portal virtualization, backend pinning, Execute integration, authoritative schema-observation runtime, complete semantic planner, or connection pool | Partial |
| `shardschema` catalog and cache | PostgreSQL 18 idempotent migration, dual-CAS route activation, commit-only notification, permanent shard restore-incarnation and logical-consumer identities, per-shard ownership fences, never-reused restore/source-bound checkpoint, attachment, consumer-slot, slot-creation-attempt, and per-shard slot-sync-probe generations, generation-encoded managed names, ownership-fenced and ordinal-CAS monotonic checkpoint progress, selected-source and primary-anchor activation boundaries, ordered attachment/slot/probe/restore retirement, live PG18 lifecycle, concurrent fence-versus-progress, and restricted-role write-path tests, owned idle LISTEN-before-load reader, bounded notification coalescing and 1-to-300-second periodic refresh driver, deadline-bounded connection and catalog-operation limits with lock-release-woken cache publication, successful boundary-publication precedence, timeout-classified boundary errors, and bounded credential-safe failure categories, immediate client teardown, and server-side transaction-timeout rollback, fresh-session reconnect with jittered bounded backoff, fail-closed initial readiness, bounded stale-cache grace plus an independent 15-minute cache ceiling, catalog-derived pooler HTTP/Prometheus publication, bounded repeatable-read routing snapshots, validated checksums and a lock-free cache owning at most eight recent epochs, plus local-only executable composition using a bounded regular DSN file. A separate dedicated Rust reader resets inherited state, pins and verifies an empty `search_path`, then loads one ready standby-selected consumer policy on demand in a read-only repeatable-read transaction and binds its cluster, restore lineage, owner fence, checkpoint ordinal, and managed-slot generations; it does not cache the whole consumer registry. Probe rows remain non-authorizing tombstones by themselves. A bounded primitive drives allocation, durable pre-dispatch intent, exact create-attempt-receipt activation and cleanup, and target-fenced absence retirement through a hidden canonical writable-`shardschema` registry with an opaque fence ID bound to the exact live backend and postmaster generation, with genesis handling, post-wait durable revalidation, stale-epoch fencing, idempotent retries, and explicit unknown-commit classification. The registry does not consume advisory-lock keys, rejects stale PID reuse, and serializes writers with a fail-fast table lock compatible with ordinary lifecycle DML. Built-in advisory-lock ACLs remain at PostgreSQL defaults because a database-local migration cannot establish a postmaster-wide hostile-SQL boundary. Related catalog lifecycle writes use the same database-enforced registry and reject unresolved attempts; catalog roles cannot inspect creation attempts, raw probe receipts, or fence state. Live role tests reject untrusted fence acquisition or inspection, unsafe fixed-role bootstrap, raw receipt reads or updates, guessed receipts, released-fence transaction-lock substitution, stale backend-generation reuse, and bounded fence release; cleanup proves the complete fixture hierarchy retired. Forward migration tests recreate a valid v0.49 probe lifecycle and a pre-ledger active/retiring consumer lifecycle, reject unreconcilable receiptless live rows instead of inventing authority, preserve allocated and legally retired history, reapply cleanly, and remove their disposable schema. Live fault injection confirms committed probe and typed consumer activation can be reconciled and replayed with the same held receipt after COMMIT response loss, a confirmed retirement COMMIT response held beyond the caller deadline preserves the original outcome-unknown classification when the same fence is proven inside a fresh verification tail, and catalog-backend loss after durable intent but before target dispatch leaves an explicit reconciliation barrier without creating a physical slot. A restore workflow/CAS API, long-running live-slot reconciliation, automatic unknown-outcome recovery, authenticated TLS, remote catalog transport, and operator credential distribution are absent | Partial |
| Cross-shard 2PC and recovery | Design only; no executable coordinator | Planned |
| Online DDL and role propagation | Design only | Planned |
| `pgoutput` change stream | Zero-copy PostgreSQL 18 envelope and `pgoutput` v1-v4 decoding, ordered progress validation, nonzero transaction IDs, two-phase and logical-message controls, a pure fail-closed standby-attachment contract, permanent `shardschema` consumer/checkpoint/attachment/slot/probe generations, and a bounded catalog-policy reader are implemented. Consumed-connection local observers pin an empty `search_path` and collect exact primary and standby slot, recovery, timeline, WAL receiver/sender, physical-slot, feedback, continuous slot-sync-worker, synchronization-gate and failover-anchor state inside bounded monotonic windows. A pure correlator requires matching observable source/database components, roles, logical WAL, mandatory `hot_standby_feedback`, continuous slot synchronization, replay-floor coverage, exact receiver/physical-slot/walsender ownership, retained WAL and compatible primary/synchronized anchor progress without claiming endpoint adjacency, freshness that PostgreSQL cannot expose, or decoder authorization. A separate consumed-connection mutator creates persistent two-phase `pgoutput` primary failover anchors after exact source/role preflight; standby-local creation additionally consumes a fresh correlated path, bounds preflight by its expiry, and rechecks its receiver PID, physical slot, live timeline, bounded feedback interval and slot-sync-worker generation at dispatch and after the effect. It persists a pre-dispatch creation barrier, pins an empty path without resetting the caller's effective role or hard-bounded advisory locks, serializes every managed create/drop through the hidden canonical target registry using an opaque backend-generation-bound fence, returns a process-local persistence/ownership receipt, and drops only the inactive unchanged shape represented by that receipt. Cleanup skips creation-only WAL, receiver, physical-slot, feedback and synchronization health. Known pre-dispatch failures and PostgreSQL 18's exact effect-free `object_in_use` active-slot rejection return the receipt; a successful exact drop returns a connection-bound absence guard carrying the same opaque create-attempt ID. Typed consumer and probe lifecycle paths pass caller-held receipts without reloading hidden capabilities; the clean slot-sync-probe path persists that ID through conditional catalog transitions and requires the continuous worker's synchronized copy to appear and disappear in live PostgreSQL 18. Every other post-dispatch error, timeout, or connection/validation failure is explicitly outcome-unknown and cannot be blindly retried. Live coverage also includes both create/verify/drop roles, a real logical-replication session raced after inactive-slot preflight, active-slot/staged-attachment cleanup, a same-name managed recreation blocked across final catalog COMMIT, catalog-backend loss after durable intent, probe and typed consumer activation COMMIT-response loss, exact reconciliation, and same-receipt replay, direct catalog lifecycle serialization, the fixed synchronized anchor, snapshot-triggered standby creation, preserved restricted-role and registry fencing, oversized-fence rejection, draining-shard rejection, returned cleanup authority after PostgreSQL 17 rejection, bounded always-run cleanup and fence release, transactional fixture setup, ephemeral test-principal teardown, primary/standby observation and correlation, restricted-role redaction, fixed-role bootstrap rejection, released-fence transaction-lock rejection, stale backend-generation reclamation, and full fixture hierarchy retirement. Runtime never calls the testing-oriented `pg_sync_replication_slots()`. Full configured-slot-set/catalog equality, exact live-replay and network-adjacency proof, restore-incarnation observation outside the probe allocator, feedback-age/horizon proof, a source-bound recent worker-cycle progress challenge, injected slot-mutation post-dispatch response-loss races, a complete post-dispatch outcome ledger and automatic unknown-outcome reconciliation, quarantined replication-connection ownership, complete transaction ordering, relation caching, durable feedback, cross-shard merge, snapshots and the embedded pooler stream runtime remain absent | Partial |
| Backup/restore and MinIO verification | Design only | Planned |
| Online resharding | Design only | Planned |
| Admin UI, Prometheus and OpenTelemetry | The pooler control executable serves catalog Prometheus exposition; scraping resources, SQL-path metrics, OpenTelemetry export, dashboards, UI, and Grafana/Tempo validation are absent | Partial |
| KIND, Jepsen/Elle and PgBouncer comparison | Targeted operator PVC delete/recreate, admission/fail-closed manager, and two-shard single-member PostgreSQL startup, Service SQL, graceful restart persistence, and binding-identity plus authenticated force-delete receipt recovery across a webhook outage KIND tests; HA traffic, history, resharding, backup and performance suites remain absent | Partial |

The current operator plan starts poolers only in explicit
`bootstrap-unavailable` catalog mode. This proves a credential-free process can
remain alive and observable before catalog transport exists; phase
`not_configured` and readiness reason `catalog_not_configured` remain
fail-closed, and no connection attempt is made. It is not evidence of a usable
pooler or database endpoint.

A limited direct-PostgreSQL development slice can be installed from the current
source. The admission Kustomization installs the CRD, manager, self-managed
serving certificate, and fail-closed webhook configurations. Direct PostgreSQL
namespaces must opt into the binding fence with the documented namespace label;
the separate certificate-free overlay is only for supporting-controller
debugging and must not manage PostgreSQL Pods. Automatic
CA rotation is not implemented. A one-member asynchronous resource creates one
PostgreSQL 18 primary and retained PVC per shard; a three- or five-member
resource still creates none. The pooler accepts no SQL session and has no
connection pool, so application Services are not usable endpoints. The direct
primary path has no HA and restarts interrupt its shard. No sharding,
availability, backup, restore, resharding, DDL, or performance guarantee is
claimed until its implementation and required tests are merged and listed here.
