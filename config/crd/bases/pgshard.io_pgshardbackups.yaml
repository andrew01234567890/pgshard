---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: pgshardbackups.pgshard.io
spec:
  group: pgshard.io
  names:
    kind: PgShardBackup
    listKind: PgShardBackupList
    plural: pgshardbackups
    singular: pgshardbackup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clusterName
      name: Cluster
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PgShardBackup is a single backup run.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              PgShardBackupSpec requests one backup of a cluster. It is immutable: a
              backup is one operation, and the physical work starts against the
              cluster and type it was created with. Editing them afterwards would
              record the result of backing up one cluster as a backup of another.
            properties:
              clusterName:
                type: string
              type:
                default: full
                enum:
                - full
                - differential
                - incremental
                type: string
            required:
            - clusterName
            type: object
            x-kubernetes-validations:
            - message: a backup's spec is immutable; create another PgShardBackup
              rule: self == oldSelf
          status:
            description: PgShardBackupStatus is the observed state of a backup.
            properties:
              backupId:
                description: BackupID is the catalog stanza's backup label once every
                  group completed.
                type: string
              clusterName:
                description: |-
                  ClusterName and Type are the spec this run was accepted with,
                  recorded before any physical work starts. Provenance is read from
                  here, never from the spec: what a completed backup protected is what
                  was captured when it began.
                type: string
              completedAt:
                format: date-time
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              error:
                type: string
              groups:
                items:
                  description: GroupBackupStatus is the outcome of one group's pgBackRest
                    backup.
                  properties:
                    backupId:
                      type: string
                    completedAt:
                      format: date-time
                      type: string
                    duration:
                      type: string
                    error:
                      type: string
                    group:
                      type: string
                    repoSizeBytes:
                      format: int64
                      type: integer
                    sizeBytes:
                      format: int64
                      type: integer
                    stanza:
                      type: string
                    startLSN:
                      type: string
                    startedAt:
                      format: date-time
                      type: string
                    stopLSN:
                      type: string
                    walStart:
                      type: string
                    walStop:
                      type: string
                  required:
                  - group
                  - stanza
                  type: object
                type: array
              phase:
                type: string
              policy:
                description: |-
                  Policy is the backup policy the run resolved, by name and UID: a
                  policy deleted and recreated at the same name is a different store.
                type: string
              policyUID:
                type: string
              startedAt:
                format: date-time
                type: string
              type:
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
