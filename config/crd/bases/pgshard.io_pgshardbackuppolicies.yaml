---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: pgshardbackuppolicies.pgshard.io
spec:
  group: pgshard.io
  names:
    kind: PgShardBackupPolicy
    listKind: PgShardBackupPolicyList
    plural: pgshardbackuppolicies
    singular: pgshardbackuppolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.objectStore.type
      name: Store
      type: string
    - jsonPath: .status.conditions[?(@.type=="BackupHealthy")].status
      name: Healthy
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PgShardBackupPolicy defines where and when clusters are backed
          up.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              PgShardBackupPolicySpec is the desired backup policy. Clusters bind to it
              through spec.backup.policyRef.
            properties:
              barrierSchedule:
                description: |-
                  BarrierSchedule is a cron expression on which the operator asks each
                  bound cluster's controller for a certified barrier: a cluster-wide
                  restore point taken with writes paused and two-phase commits drained.
                type: string
              controllerEndpoint:
                description: |-
                  ControllerEndpoint is the host:port of a cluster's Controller gRPC
                  service, with {cluster} and {namespace} substituted; the default is
                  {cluster}-controller.{namespace}.svc:15500.
                type: string
              logLevel:
                description: |-
                  LogLevel is the pgBackRest log level (off, error, warn, info, detail,
                  debug, trace).
                enum:
                - "off"
                - error
                - warn
                - info
                - detail
                - debug
                - trace
                type: string
              objectStore:
                description: |-
                  ObjectStoreSpec locates a backup repository.
                  The fields each store variant needs are required here as well as at
                  runtime, so an object that cannot work is refused when it is written
                  rather than accepted and then reported invalid. The rules mirror
                  backup.Settings.Validate; the credential ones account for the same
                  defaulting it applies, where an unset credentialType means shared for
                  s3 and azure and service for gcs.
                  These fields are written into pgbackrest.conf as key=value lines, so a
                  value carrying a newline writes an option of its own: an endpoint of the
                  attacker's choosing takes every backup and WAL segment with it, and the
                  backups are the one artifact holding a complete copy of all tenant data.

                  One rule rather than one per field, and contains() rather than a regex:
                  Kubernetes gives each object's validation a cost budget, and past it the
                  API server stops evaluating rules rather than refusing the object -- so
                  five regex rules here silently disabled the credential rules below.
                properties:
                  bucket:
                    description: Bucket is the S3 or GCS bucket.
                    type: string
                  container:
                    description: Container is the Azure blob container.
                    type: string
                  credentialType:
                    description: |-
                      CredentialType selects how pgBackRest authenticates:
                      s3 shared|web-id|auto, azure shared|sas, gcs service|token|auto.
                    enum:
                    - shared
                    - web-id
                    - auto
                    - sas
                    - service
                    - token
                    type: string
                  credentials:
                    description: |-
                      Credentials names the Secret whose keys hold the store credentials:
                      s3 key/keySecret, azure account/key, gcs key.json, sftp privateKey.
                    properties:
                      secretRef:
                        description: |-
                          LocalObjectReference contains enough information to let you locate the
                          referenced object inside the same namespace.
                        properties:
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                    type: object
                  encryption:
                    description: |-
                      Encryption names the Secret whose passphrase key encrypts the
                      repository with aes-256-cbc.
                    properties:
                      secretRef:
                        description: |-
                          LocalObjectReference contains enough information to let you locate the
                          referenced object inside the same namespace.
                        properties:
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                    type: object
                  endpoint:
                    description: |-
                      Endpoint overrides the store endpoint; an http:// or https:// URL or a
                      host[:port].
                    type: string
                  insecureUnencrypted:
                    description: |-
                      InsecureUnencrypted stores the repository in the clear. A backup is a
                      complete copy of every tenant's data, and a remote store is somewhere
                      else's disk, so encryption is required for one unless this says
                      otherwise -- the same shape as internalTLS.insecure. A posix
                      repository is a PVC of this cluster's own and is exempt.
                    type: boolean
                  prefix:
                    description: Prefix is the path inside the store the repository
                      lives under.
                    type: string
                  region:
                    type: string
                  sftp:
                    description: SFTP holds the host settings of an sftp store.
                    properties:
                      host:
                        type: string
                      hostKeyCheck:
                        enum:
                        - strict
                        - accept-new
                        - fingerprint
                        - none
                        type: string
                      port:
                        type: integer
                      user:
                        type: string
                    required:
                    - host
                    - user
                    type: object
                    x-kubernetes-validations:
                    - message: sftp.host must not contain a newline, a carriage return
                        or an equals sign
                      rule: '!(self.host.contains(''\n'') || self.host.contains(''\r'')
                        || self.host.contains(''=''))'
                    - message: sftp.user must not contain a newline, a carriage return
                        or an equals sign
                      rule: '!(self.user.contains(''\n'') || self.user.contains(''\r'')
                        || self.user.contains(''=''))'
                  type:
                    enum:
                    - s3
                    - azure
                    - gcs
                    - posix
                    - sftp
                    type: string
                  uriStyle:
                    description: URIStyle is host or path addressing for S3 and Azure.
                    enum:
                    - host
                    - path
                    type: string
                  verifyTLS:
                    description: VerifyTLS defaults to true; false accepts self-signed
                      store certificates.
                    type: boolean
                required:
                - type
                type: object
                x-kubernetes-validations:
                - message: an s3 store needs bucket, endpoint and region
                  rule: self.type != 's3' || (has(self.bucket) && has(self.endpoint)
                    && has(self.region))
                - message: s3 shared credentials need credentials.secretRef
                  rule: self.type != 's3' || has(self.credentialType) && self.credentialType
                    != 'shared' || has(self.credentials.secretRef)
                - message: an azure store needs container
                  rule: self.type != 'azure' || has(self.container)
                - message: an azure store needs credentials.secretRef
                  rule: self.type != 'azure' || has(self.credentials.secretRef)
                - message: a gcs store needs bucket
                  rule: self.type != 'gcs' || has(self.bucket)
                - message: gcs service and token credentials need credentials.secretRef
                  rule: self.type != 'gcs' || has(self.credentialType) && !(self.credentialType
                    in ['service', 'token']) || has(self.credentials.secretRef)
                - message: an sftp store needs sftp.host and sftp.user
                  rule: self.type != 'sftp' || has(self.sftp)
                - message: an sftp store needs credentials.secretRef
                  rule: self.type != 'sftp' || has(self.credentials.secretRef)
                - message: bucket must not contain a newline, a carriage return or
                    an equals sign
                  rule: (!has(self.bucket) || !(self.bucket.contains('\n') || self.bucket.contains('\r')
                    || self.bucket.contains('=')))
                - message: container must not contain a newline, a carriage return
                    or an equals sign
                  rule: (!has(self.container) || !(self.container.contains('\n') ||
                    self.container.contains('\r') || self.container.contains('=')))
                - message: endpoint must not contain a newline, a carriage return
                    or an equals sign
                  rule: (!has(self.endpoint) || !(self.endpoint.contains('\n') ||
                    self.endpoint.contains('\r') || self.endpoint.contains('=')))
                - message: region must not contain a newline, a carriage return or
                    an equals sign
                  rule: (!has(self.region) || !(self.region.contains('\n') || self.region.contains('\r')
                    || self.region.contains('=')))
                - message: prefix must not contain a newline, a carriage return or
                    an equals sign
                  rule: (!has(self.prefix) || !(self.prefix.contains('\n') || self.prefix.contains('\r')
                    || self.prefix.contains('=')))
                - message: a remote repository must set encryption.secretRef, or insecureUnencrypted
                    to store every tenant's data in the clear on purpose
                  rule: self.type == 'posix' || (has(self.insecureUnencrypted) &&
                    self.insecureUnencrypted) || (has(self.encryption) && has(self.encryption.secretRef))
              processMax:
                description: ProcessMax bounds pgBackRest parallelism per member.
                minimum: 1
                type: integer
              retention:
                description: BackupRetention counts backups to keep per type.
                properties:
                  differential:
                    minimum: 1
                    type: integer
                  full:
                    minimum: 1
                    type: integer
                type: object
              schedules:
                description: BackupSchedules holds cron expressions per backup type.
                properties:
                  differential:
                    type: string
                  full:
                    type: string
                  incremental:
                    type: string
                type: object
            required:
            - objectStore
            type: object
          status:
            description: PgShardBackupPolicyStatus is the observed state of a policy.
            properties:
              accepted:
                description: |-
                  Accepted is the last spec that validated, and AcceptedGeneration the
                  generation it came from. Members archive to this rather than to
                  spec: an edit that does not validate must not reach the pods, and
                  the alternative to keeping what was accepted is rolling every member
                  onto no archive configuration at all.
                properties:
                  barrierSchedule:
                    description: |-
                      BarrierSchedule is a cron expression on which the operator asks each
                      bound cluster's controller for a certified barrier: a cluster-wide
                      restore point taken with writes paused and two-phase commits drained.
                    type: string
                  controllerEndpoint:
                    description: |-
                      ControllerEndpoint is the host:port of a cluster's Controller gRPC
                      service, with {cluster} and {namespace} substituted; the default is
                      {cluster}-controller.{namespace}.svc:15500.
                    type: string
                  logLevel:
                    description: |-
                      LogLevel is the pgBackRest log level (off, error, warn, info, detail,
                      debug, trace).
                    enum:
                    - "off"
                    - error
                    - warn
                    - info
                    - detail
                    - debug
                    - trace
                    type: string
                  objectStore:
                    description: |-
                      ObjectStoreSpec locates a backup repository.
                      The fields each store variant needs are required here as well as at
                      runtime, so an object that cannot work is refused when it is written
                      rather than accepted and then reported invalid. The rules mirror
                      backup.Settings.Validate; the credential ones account for the same
                      defaulting it applies, where an unset credentialType means shared for
                      s3 and azure and service for gcs.
                      These fields are written into pgbackrest.conf as key=value lines, so a
                      value carrying a newline writes an option of its own: an endpoint of the
                      attacker's choosing takes every backup and WAL segment with it, and the
                      backups are the one artifact holding a complete copy of all tenant data.

                      One rule rather than one per field, and contains() rather than a regex:
                      Kubernetes gives each object's validation a cost budget, and past it the
                      API server stops evaluating rules rather than refusing the object -- so
                      five regex rules here silently disabled the credential rules below.
                    properties:
                      bucket:
                        description: Bucket is the S3 or GCS bucket.
                        type: string
                      container:
                        description: Container is the Azure blob container.
                        type: string
                      credentialType:
                        description: |-
                          CredentialType selects how pgBackRest authenticates:
                          s3 shared|web-id|auto, azure shared|sas, gcs service|token|auto.
                        enum:
                        - shared
                        - web-id
                        - auto
                        - sas
                        - service
                        - token
                        type: string
                      credentials:
                        description: |-
                          Credentials names the Secret whose keys hold the store credentials:
                          s3 key/keySecret, azure account/key, gcs key.json, sftp privateKey.
                        properties:
                          secretRef:
                            description: |-
                              LocalObjectReference contains enough information to let you locate the
                              referenced object inside the same namespace.
                            properties:
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                            type: object
                            x-kubernetes-map-type: atomic
                        type: object
                      encryption:
                        description: |-
                          Encryption names the Secret whose passphrase key encrypts the
                          repository with aes-256-cbc.
                        properties:
                          secretRef:
                            description: |-
                              LocalObjectReference contains enough information to let you locate the
                              referenced object inside the same namespace.
                            properties:
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                            type: object
                            x-kubernetes-map-type: atomic
                        type: object
                      endpoint:
                        description: |-
                          Endpoint overrides the store endpoint; an http:// or https:// URL or a
                          host[:port].
                        type: string
                      insecureUnencrypted:
                        description: |-
                          InsecureUnencrypted stores the repository in the clear. A backup is a
                          complete copy of every tenant's data, and a remote store is somewhere
                          else's disk, so encryption is required for one unless this says
                          otherwise -- the same shape as internalTLS.insecure. A posix
                          repository is a PVC of this cluster's own and is exempt.
                        type: boolean
                      prefix:
                        description: Prefix is the path inside the store the repository
                          lives under.
                        type: string
                      region:
                        type: string
                      sftp:
                        description: SFTP holds the host settings of an sftp store.
                        properties:
                          host:
                            type: string
                          hostKeyCheck:
                            enum:
                            - strict
                            - accept-new
                            - fingerprint
                            - none
                            type: string
                          port:
                            type: integer
                          user:
                            type: string
                        required:
                        - host
                        - user
                        type: object
                        x-kubernetes-validations:
                        - message: sftp.host must not contain a newline, a carriage
                            return or an equals sign
                          rule: '!(self.host.contains(''\n'') || self.host.contains(''\r'')
                            || self.host.contains(''=''))'
                        - message: sftp.user must not contain a newline, a carriage
                            return or an equals sign
                          rule: '!(self.user.contains(''\n'') || self.user.contains(''\r'')
                            || self.user.contains(''=''))'
                      type:
                        enum:
                        - s3
                        - azure
                        - gcs
                        - posix
                        - sftp
                        type: string
                      uriStyle:
                        description: URIStyle is host or path addressing for S3 and
                          Azure.
                        enum:
                        - host
                        - path
                        type: string
                      verifyTLS:
                        description: VerifyTLS defaults to true; false accepts self-signed
                          store certificates.
                        type: boolean
                    required:
                    - type
                    type: object
                    x-kubernetes-validations:
                    - message: an s3 store needs bucket, endpoint and region
                      rule: self.type != 's3' || (has(self.bucket) && has(self.endpoint)
                        && has(self.region))
                    - message: s3 shared credentials need credentials.secretRef
                      rule: self.type != 's3' || has(self.credentialType) && self.credentialType
                        != 'shared' || has(self.credentials.secretRef)
                    - message: an azure store needs container
                      rule: self.type != 'azure' || has(self.container)
                    - message: an azure store needs credentials.secretRef
                      rule: self.type != 'azure' || has(self.credentials.secretRef)
                    - message: a gcs store needs bucket
                      rule: self.type != 'gcs' || has(self.bucket)
                    - message: gcs service and token credentials need credentials.secretRef
                      rule: self.type != 'gcs' || has(self.credentialType) && !(self.credentialType
                        in ['service', 'token']) || has(self.credentials.secretRef)
                    - message: an sftp store needs sftp.host and sftp.user
                      rule: self.type != 'sftp' || has(self.sftp)
                    - message: an sftp store needs credentials.secretRef
                      rule: self.type != 'sftp' || has(self.credentials.secretRef)
                    - message: bucket must not contain a newline, a carriage return
                        or an equals sign
                      rule: (!has(self.bucket) || !(self.bucket.contains('\n') ||
                        self.bucket.contains('\r') || self.bucket.contains('=')))
                    - message: container must not contain a newline, a carriage return
                        or an equals sign
                      rule: (!has(self.container) || !(self.container.contains('\n')
                        || self.container.contains('\r') || self.container.contains('=')))
                    - message: endpoint must not contain a newline, a carriage return
                        or an equals sign
                      rule: (!has(self.endpoint) || !(self.endpoint.contains('\n')
                        || self.endpoint.contains('\r') || self.endpoint.contains('=')))
                    - message: region must not contain a newline, a carriage return
                        or an equals sign
                      rule: (!has(self.region) || !(self.region.contains('\n') ||
                        self.region.contains('\r') || self.region.contains('=')))
                    - message: prefix must not contain a newline, a carriage return
                        or an equals sign
                      rule: (!has(self.prefix) || !(self.prefix.contains('\n') ||
                        self.prefix.contains('\r') || self.prefix.contains('=')))
                    - message: a remote repository must set encryption.secretRef,
                        or insecureUnencrypted to store every tenant's data in the
                        clear on purpose
                      rule: self.type == 'posix' || (has(self.insecureUnencrypted)
                        && self.insecureUnencrypted) || (has(self.encryption) && has(self.encryption.secretRef))
                  processMax:
                    description: ProcessMax bounds pgBackRest parallelism per member.
                    minimum: 1
                    type: integer
                  retention:
                    description: BackupRetention counts backups to keep per type.
                    properties:
                      differential:
                        minimum: 1
                        type: integer
                      full:
                        minimum: 1
                        type: integer
                    type: object
                  schedules:
                    description: BackupSchedules holds cron expressions per backup
                      type.
                    properties:
                      differential:
                        type: string
                      full:
                        type: string
                      incremental:
                        type: string
                    type: object
                required:
                - objectStore
                type: object
              acceptedGeneration:
                format: int64
                type: integer
              clusters:
                description: |-
                  Clusters lists every cluster whose spec.backup.policyRef names this
                  policy with its last successful backups.
                items:
                  description: ClusterBackupStatus is the backup health of one cluster
                    bound to a policy.
                  properties:
                    healthy:
                      type: boolean
                    lastDifferentialTime:
                      format: date-time
                      type: string
                    lastFullTime:
                      format: date-time
                      type: string
                    lastIncrementalTime:
                      format: date-time
                      type: string
                    message:
                      type: string
                    name:
                      type: string
                  required:
                  - healthy
                  - name
                  type: object
                type: array
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
