---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: pgshardreshards.pgshard.io
spec:
  group: pgshard.io
  names:
    kind: PgShardReshard
    listKind: PgShardReshardList
    plural: pgshardreshards
    singular: pgshardreshard
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clusterName
      name: Cluster
      type: string
    - jsonPath: .spec.targetShards
      name: Shards
      type: integer
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PgShardReshard is the record of one resharding run.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              PgShardReshardSpec records one change of the shard map: the generation it
              starts from and the target ranges of the pending shard set. The catalog
              table pgshard.shard_ranges is the source of truth; this object mirrors it.
            properties:
              clusterName:
                type: string
              fromGeneration:
                description: FromGeneration is the serving shard set generation being
                  replaced.
                format: int64
                minimum: 1
                type: integer
              targetGeneration:
                description: |-
                  TargetGeneration is the pending shard set generation; its catalog
                  name is targetShardSet.
                format: int64
                minimum: 2
                type: integer
              targetRanges:
                items:
                  description: |-
                    ReshardRange is one target shard and the inclusive keyspace interval it
                    will own.
                  properties:
                    rangeEnd:
                      format: int64
                      type: integer
                    rangeStart:
                      format: int64
                      type: integer
                    shardId:
                      minimum: 0
                      type: integer
                  required:
                  - rangeEnd
                  - rangeStart
                  - shardId
                  type: object
                minItems: 1
                type: array
              targetShardSet:
                minLength: 1
                type: string
              targetShards:
                minimum: 1
                type: integer
            required:
            - clusterName
            - fromGeneration
            - targetGeneration
            - targetRanges
            - targetShardSet
            - targetShards
            type: object
            x-kubernetes-validations:
            - message: targetRanges must list exactly targetShards ranges
              rule: size(self.targetRanges) == self.targetShards
            - message: targetGeneration must be greater than fromGeneration
              rule: self.targetGeneration > self.fromGeneration
          status:
            description: PgShardReshardStatus is the observed state of a reshard.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              journalIds:
                items:
                  type: string
                type: array
              message:
                type: string
              phase:
                enum:
                - Pending
                - Provisioning
                - Copying
                - Verifying
                - Switching
                - Completed
                - Cancelled
                - Failed
                type: string
              targets:
                items:
                  description: ReshardTargetStatus is the readiness of one target
                    group.
                  properties:
                    group:
                      type: string
                    primary:
                      type: string
                    ready:
                      type: boolean
                    shardId:
                      type: integer
                  required:
                  - group
                  - shardId
                  type: object
                type: array
              workflowId:
                description: WorkflowID is the pgshard.workflows row driving the reshard.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
