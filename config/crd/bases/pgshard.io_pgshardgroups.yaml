---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: pgshardgroups.pgshard.io
spec:
  group: pgshard.io
  names:
    kind: PgShardGroup
    listKind: PgShardGroupList
    plural: pgshardgroups
    singular: pgshardgroup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.kind
      name: Kind
      type: string
    - jsonPath: .status.primary
      name: Primary
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PgShardGroup is a status-only view of one catalog or shard group.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PgShardGroupSpec identifies the replication group this object
              mirrors.
            properties:
              clusterRef:
                type: string
              kind:
                enum:
                - catalog
                - shard
                type: string
              shardId:
                type: integer
            required:
            - clusterRef
            - kind
            type: object
          status:
            description: PgShardGroupStatus is the observed state of a replication
              group.
            properties:
              epoch:
                format: int64
                type: integer
              members:
                items:
                  description: MemberStatus reports one PostgreSQL instance in a group.
                  properties:
                    name:
                      type: string
                    ready:
                      type: boolean
                    replayLagBytes:
                      format: int64
                      type: integer
                    role:
                      type: string
                  required:
                  - name
                  type: object
                type: array
              primary:
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
