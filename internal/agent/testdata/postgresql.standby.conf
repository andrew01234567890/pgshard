# Managed by pgshard-agent. Edits are overwritten.
archive_timeout = '5min'
hot_standby = on
hot_standby_feedback = on
listen_addresses = '*'
max_active_replication_origins = 16
max_prepared_transactions = 200
max_replication_slots = 16
max_wal_senders = 16
password_encryption = 'scram-sha-256'
port = 5432
primary_conninfo = 'host=s0-0 port=5432 user=postgres dbname=postgres application_name=s0-1'
primary_slot_name = 'pgshard_s0_1'
restart_after_crash = off
ssl = off
synchronous_commit = on
synchronous_standby_names = 'ANY 1 (s0-1, s0-2)'
track_commit_timestamp = on
unix_socket_directories = '/tmp'
wal_keep_size = '512MB'
wal_level = logical
wal_log_hints = on
wal_receiver_timeout = '5s'
wal_sender_timeout = '5s'
include_if_exists = 'pgshard.override.conf'
