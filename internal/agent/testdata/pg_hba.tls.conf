# Managed by pgshard-agent. Edits are overwritten.
local   all             all                                     scram-sha-256
local   replication     all                                     scram-sha-256

# TLS is preferred; plain TCP is rejected when certificates are configured.
hostssl  all             postgres        10.244.0.0/16           scram-sha-256
hostssl  replication     postgres        10.244.0.0/16           scram-sha-256
hostssl  all             all             10.244.0.0/16           reject
hostnossl all             all             10.244.0.0/16           reject
