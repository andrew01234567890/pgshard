// Package policy holds tests that assert conventions across the whole tree
// rather than behaviour within one package. A convention nothing checks is
// a convention that decays: it holds in the code that was reviewed when it
// was written, and the next contributor has no way to know it exists.
package policy
